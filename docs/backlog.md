# backlog.md — 技術バックログ

実装を先送りした項目の追跡リスト。着手時は担当を決めて Issue / PR に移すこと。

| # | 項目 | 内容 | 着手目安 |
|---|---|---|---|
| 1 | ADR-005 暗号化の本実装 | **実装済み（2026-07-03）**: `functions/api/credentials.ts` が AES-GCM 暗号化して `user_credentials` に保管（AI設定・BYOK とも、ログイン時はアカウント保存）。残タスク: 鍵ローテーション手順の文書化、localStorage フォールバック分の移行導線 | 残タスクのみ |
| 3 | AIゲートウェイの実装（ADR-008） | **実装済み（2026-07-04）**: `/api/ai` がサーバ側でプロバイダを呼び、**利用量記録（`ai_usage` 表・NFR-05）／レート制限（1時間120回・本人キー保護で 429）／モデル許可リスト**（`ALLOWED_MODELS`）を備える。migration `20260704090000`。残: モデル一覧の追随（#7）、上限値のadmin調整UI（現状は定数） | 実装済み |
| 2 | ADR-007（GAS橋方式）の起草 | **完了（2026-07-03、architecture.md ADR-007 として起草）** | 完了 |
| 8 | 独自SMTPの設定（メンバー招待前に必須） | **設定済み（2026-07-04）**: Brevo（無料枠300通/日）を Supabase（Authentication → Emails → Custom SMTP）に接続。送信元 `yo-mukai@honmono-all.com`（Sender name: NAGAYA-BASE）、host `smtp-relay.brevo.com:587`。送信レート上限（Rate Limits → Emails per hour）を 100 に引き上げ済み。認証情報は Supabase 管理画面のみ（リポジトリ非保管）。**残タスク（任意・優先度低）**: honmono-all.com のドメイン認証（DKIM）。未認証のため From が Brevo サブドメイン（`…@11591849.brevosend.com`）に書き換わり、honmono-all.com は Reply-To のみ。DMARC は `p=none` のため到達は問題なし（受信トレイ着）。DNS は **Wix**（ns0/ns1.wixdns.net）管理 → Brevo「Ask someone else to authenticate」でサイト担当に依頼可。身内運用のうちは現状維持で可 | 設定済み／DKIM は運営調整後 |
| 5 | 開発フローの PR 全面移行（A案） | **建立会（メンバー初参加日）当日に実施**。それまでは Claude Code / 管理者の作業は main への直 push（B案）を継続する（admin バイパスの "Bypassed rule violations" 警告は既知・許容）。建立会をもって全作業を PR 経由に切り替え、直 push を停止する（決定日: 2026-07-03） | 建立会当日 |
| 7 | gadget.ai の複数プロバイダ対応 | **完了（2026-07-04）**: `/api/ai` が anthropic/openai/google を各社形式に変換して呼ぶ。AI設定にプロバイダ選択、案内所に「AIの使い方」ページ（各社キー取得手順）。残: OpenAI互換の任意エンドポイント対応、モデル一覧の追随 | 追加要望を見て |
| 6 | 交流レイヤー・フェーズ2（双方向） | メッセージボード・コメント・在室/オンライン表示・相互通知・リアクション等の**双方向機能は意図的に未実装**（過疎の可視化を避ける設計原則）。**着手条件: 実働入居者が増え、一方向の情報（回覧板・速報・歩み）に自然な反応が生まれ始めた時**。フェーズ1（一方向: 回覧板/速報！/長屋暦/案内所/長屋の歩み）は 2026-07-04 実装完了でクローズ。メール/プッシュ通知も同様に保留 | 条件成立まで保留 |
| 4 | AIキーの Function 代理実行（/api/ai） | **完了（2026-07-04 時点で実装済みを確認）**: `/api/ai` はキーを Function 内でのみ復号し各社APIを呼ぶ。**復号済みキーはクライアントに一切返さない**（クライアントは生成テキストと非秘匿メタのみ受領）。#3 と同じゲートウェイに統合済み | 完了 |
| 10 | 道具の緊急停止UI（FR-09） | **実装済み（2026-07-04）**: 大家の間に「道具の緊急停止」。全道具一覧＋停止(`suspended`)/再開(`published`)。`/api/admin` の action `set-gadget-status`（service_role・audit_logs 記録）。RLS でも admin のみ許可の二重防御。migration 不要 | 実装済み |
| 11 | 依存の脆弱性（Dependabot） | **対応（2026-07-04）**: Dependabot の指摘（約32件）はほぼ全てが `tools/setup-wizard` の Electron（本番Pages/Workersは非搭載）。Electron を `^43.0.0` へ更新し `npm audit` は 0 件に。残: 実機での wizard パッケージング（electron-builder）確認は配布前に一度 | 更新済み／配布前に実機確認 |
| 9 | 退去（本人によるアカウント削除） | **実装済み（2026-07-04）**: 入居者情報の最下部に危険操作エリア。個人データ（profiles/installations/gadget_storage/user_credentials）は必ず削除。作った道具の扱いは本人が2択（**既定=長屋に残す**: `owner_id`→null で大家預かり・公開維持／**下げる**: `status='suspended'`）。auth 削除は `/api/leave`（service_role）でのみ実行。**著作権はUIに一切出さない（ADR-006 現状維持: 著作権は作者に残る／CLA許諾は撤回不可。放棄させると純OSS化と同じ問題でB2B価値を毀損）**。最後の大家は退去不可（管理者不在防止）。DB: migration `20260704080000` で profiles 参照の6 FK を `on delete set null` 化。**大家によるアカウント削除**も追加（2026-07-04）: 大家の間の各入居者に「削除」＋対象メール打ち込みの多段確認、`/api/admin` action `delete-user`（service_role・audit・最後の大家は不可・自分は不可）。誤登録/死にアカウントの掃除に使う。残タスク（任意）: 道具の完全削除に大家承認を挟む導線、退去後の惜別画面 | 実装済み |
| 12 | 案内AI 段2（文脈追従・操作補助） | **実装済み（2026-07-05）**: 2a 文脈追従（クライアントが今の画面を guide に渡す）／2b 承認つき操作補助（guide が `nagaya-action` を提案→窓が確認ボタン化→押下で install/open/help/ai-settings を実行。AIは実行しない・プラットフォーム操作のみ・ADR-001）／2c 伴走（連携設定が要る導入済み道具の「設定方法」チップ）。パーサ host/guideActions.ts（テスト付き）。記憶はしない（段3）。残: 連携クレデンシャル状態まで見た厳密な進捗追跡 | 実装済み |
| 13 | 案内AI 段3（永続記憶マネージャー） | ADR-010 段3。セッション跨ぎの永続記憶（多段階記憶・記憶圧縮・人物像更新）。**要・権利/プライバシー整理と創業者条項**。目的地だが慎重に | 権利整理後 |
| 14 | RAG の CI 自動再インデックス | **実装済み（2026-07-05）**: `.github/workflows/reindex.yml`。main への .md push で GitHub Actions が `reindex.mjs` を実行（journal/backlog/dist は除外）。**要: GitHub Secrets 3つ**（PLATFORM_EMBEDDING_KEY / SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY）。手動実行(workflow_dispatch)も可。残: ガジェットのDB公開(status変更)は発火しない（ファイルpush時に索引済みのため段1では可） | Secrets登録で有効 |
| 15 | 共通埋め込みキーの協会引き取り | 段1のプラットフォーム保有 埋め込みキー（＋キーなし職人の生成代行分）の費用を、将来 Honmono 運営へ引き取り精算する。根拠は `ai_usage`（key_owner='platform'）の集計 | 運営移管時 |
| 16 | 統合アシスタント＝ガジェットのツール開放（ADR-011） | **実装済み P2/P3（2026-07-05・gadget-spec v1.6）**: manifest `aiTools`＋permission `ai-tools`＋`gadget.ai.onToolInvoke`／ホスト→ガジェット `tool-invoke` RPC／案内AIのタグ方式ツールループ（read自動・最大3連鎖／act承認ボタン）／棚に開いているガジェットのツールのみ対象／スケジュール秘書で実証（list_events/create_event）。opt-in・既定OFF・`gadget.ai`と共存・ステートレス。残: P4 PC個別会話・段3記憶統合／複数ガジェット同時が開いていない場合の呼び出し（現状は開いている道具のみ）／実機での通しE2E確認 | P4は後続 |
| 18 | 案内所に「学びの3段階」を明文化 | **実装済み（2026-07-05）**: 案内所に記事「学びの3段階」（`06-manabi.md`）を追加。一歩目=非AI／二歩目=AI搭載(BYOK)／三歩目=案内AIに操作許可・ツール口設計。店子/職人の対称、BYOKは二歩目/三歩目の共通入口。残（任意）: 入口導線からの誘導強化 | 実装済み |
| 17 | gadget.ai の用途ヒント(tier)＋streaming（ADR-008 発展） | **tier 実装済み（2026-07-05・spec v1.6）**: `gadget.ai.complete({tier:'fast'|'smart'})` を追加、`/api/ai` が `TIER_MODEL[tier][provider]` で具体モデルに解決（省略時は従来のユーザー設定）。**残: streaming**（iframe へ postMessage 小分け配信＝`gadget.ai.stream()`。Functions のストリーミング＋SDK受け口）。現行の許可リスト/既定/不明処理は検証・コスト用として存置 | streaming が残 |
